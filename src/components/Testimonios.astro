---
const TESTIMONIOS = [
	{
		nombre: "Matias Sanchez",
		cargo: "Coach Fitness",
		texto: "El salto de calidad que tuve en el diseño y calidad de mi Landing Page con Tomi fue increíble.",
		img: "tomas-costa-testimonio-2.jpg",
	},
	{
		nombre: "Alexis Mois",
		cargo: "Agencia de Marketing",
		texto: "La dedicación, el orden y el cuidado por los detalles hacen que trabajo de Tomás sea una excelente opción para trabajar con nuestra agencia. Realmente un placer.",
		img: "tomas-costa-testimonio-1.jpg",
	},
	{
		nombre: "Vicente Calderon",
		cargo: "Agencia para consultores",
		texto: "Un gustazo trabajar con Tomas, la gran combinación de calidad y rapidez, en 7 días logró crear exactamente la idea que tenía en mi mente de mi página web. Destacando que estuvo 100% a disposición para perfeccionar cada detalle que un “cliente exigente” como yo le podía pedir, un excelente servicio 👌",
		img: "tomas-costa-testimonio-3.jpg",
	},
	{
		nombre: "Agustin Oroquieta",
		cargo: "Agencia de Marketing",
		texto: "Queríamos mejorar el diseño de nuestra web y en diez días ya la teníamos funcionando. Además instaló Google Analytics y quedó súper disponible para todo lo que necesitábamos.",
		img: "tomas-costa-testimonio-4.jpg",
	},
	{
		nombre: "Lucas Reale",
		cargo: "Coach Fitness",
		texto: "Mi experiencia con tu servicio fue 10/10. Sinceramente desde el primer día que comenzamos a trabajar vi mucho profesionalismo, compromiso y dedicación. Constantemente me ibas contando, preguntando, mostrando, etc. Eso me hacía tener mucha claridad. Muchas gracias por la buena onda y la atención 🙌🏼",
		img: "tomas-costa-testimonio-5.jpg",
	},
];
---

<section class="py-[90px] bg-[#111] rounded-[60px] overflow-clip">
	<div class="tcp-container">
		<h2
			class="mb-8 font-bold text-[32px] text-[#111] text-center leading-[110%]"
		>
			Pero... No son solo
			<br />
			<span class="--fade-up text-[#4F39F6]">Páginas Web</span>
		</h2>
		<div
			class="swiper swiperTestimonios mt-8"
			style="overflow: visible !important;"
		>
			<div class="trigger --1"></div>
			<div class="swiper-wrapper">
				{
					TESTIMONIOS.map((testimonio) => (
						<div class="testimonio-card swiper-slide bg-[#fff] rounded-xl border border-white/20 p-8">
							<div class="flex flex-col justify-between w-full min-h-[350px]">
								<p class="text-[#111]/80">
									"{testimonio.texto}"
								</p>
								<div class="flex items-center gap-4 mt-6">
									<img
										class="size-[60px] rounded-full border border-white/20"
										src={`/images/${testimonio.img}`}
										alt={`Testimonio de ${testimonio.nombre}`}
									/>
									<div class="text-[#111]/80 text-sm">
										<p class="font-bold">
											{testimonio.nombre}
										</p>
										<p>{testimonio.cargo}</p>
									</div>
								</div>
							</div>
						</div>
					))
				}
			</div>
		</div>
		<a class="tcp-btn to-animate mt-12 mx-auto" data-trigger="1" href="#planes">
			<span>Ver Paquetes »</span>
			<span class="--temblor font-medium text-[14px] block mt-1"
				>*Solo 2 cupos por semana</span
			>
		</a>
	</div>
</section>

<script is:inline>
	document.addEventListener("DOMContentLoaded", () => {
	  const btns = document.querySelectorAll(".tcp-btn.to-animate");
  
	  btns.forEach((btn) => {
		const trigger = document.querySelector(".trigger.--" + btn.dataset.trigger);
		if (!trigger) return;
  
		const observer = new IntersectionObserver(
		  ([entry]) => {
			if (entry.isIntersecting) {
			  btn.classList.add("animate");
			}
		  },
		  { threshold: 0.2 }
		);
  
		observer.observe(trigger);
	  });
	});
  </script>
